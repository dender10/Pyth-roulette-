# Testnet-only private key for Pyth Entropy on OP Sepolia
# Without this, the app runs in demo mode (client-side randomness)
# With this, the app uses real Pyth Entropy on-chain randomness
VITE_PRIVATE_KEY=0xYOUR_TESTNET_PRIVATE_KEY_HERE
